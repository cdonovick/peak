dist: xenial
language: python
python:
- 3.7
cache:
  apt: true
addons:
  apt:
    packages:
    - libgmp-dev
    - libmpfr-dev
    - libmpc-dev
before_install:
- pip install -U pip
install:
- pip install pytest-cov fault mantle
- pip install -e .
script:
- py.test --cov=peak tests/
